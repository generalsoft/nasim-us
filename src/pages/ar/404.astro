---
import Hero from '../../components/Hero.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { t } from '../../i18n/utils';

const locale = 'ar';
---

<BaseLayout title={t(locale, '404.title')} description={t(locale, '404.desc')}>
	<Hero title={t(locale, '404.heading')} tagline={t(locale, '404.tagline')} />
</BaseLayout>
